fastapi==0.104.1
uvicorn[standard]==0.24.0

# Supabase client with compatible versions (let pip resolve dependencies)
supabase==2.0.3

# Let pip resolve compatible versions for HTTP dependencies
# (supabase 2.0.3 will automatically pull compatible httpx, httpcore, etc.)
websockets>=12.0,<13.0

# OpenAI for transcription and summarization
openai==1.3.0

# FastAPI dependencies
python-multipart==0.0.6
pydantic==2.5.0

# Environment and configuration
python-dotenv==1.0.0

# Text processing and chunking
langchain==0.1.0
